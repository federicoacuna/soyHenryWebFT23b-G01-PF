import { } from '../actions'

const initialState = {

}

const reducer = (state = initialState, action) => {
  const { payload, type } = action
  switch (type) {
    default:
      return state
  }
}

export default reducer
